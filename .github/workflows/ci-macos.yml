name: CI macOS

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  brew-test:
    uses: ./.github/workflows/brew-test.yml
    with:
      runner_os: macos-latest
